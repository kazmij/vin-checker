{% extends '@AppAdmin/layout.html.twig' %}

{% block content %}
    <iframe allowfullscreen="true" style="width: 100%; height: 600px; border: none; padding:0; margin: 0; overflow: visible" src="{{ path('elfinder', {instance: 'inline'}) }}"/>
{% endblock %}

